<?php //xmlTest00.php
define ('THIS_PAGE', basename($_SERVER['PHP_SELF']));

/**
 *
 * Story the problem (Puedo code)
 *
 * Step 1: Define Criteria
 * Add what you want it to do (doesn't have to be in right order here)
 *
 * If the array exists in the session - add the players
 * If array doesn't exist, make array AND add player to array
 * IF SESSION not var - investigate?
 *
 *
 * Step 2: Review criteria
 * - discard 'IF SESSION not var - investigate? '
 *
 *
 * Step 3: Define deliverables
 * Sequence in order
 *
 * If no array exists, create
 * Then, Add data (Heroes, Ballplayers, whatever)
 *
 **/


